<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.myapplication.ViewAttendanceActivity">

    <Button
        android:id="@+id/total_attendance"
        android:textColor="#FFFFFF"
        android:layout_height="150dp"
        android:layout_width="150dp"
        android:text="@string/total_attendance"
        android:background="@drawable/blue_round_button"
        android:textSize="25sp"
        android:onClick="onTotalAttendance"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />
   <Button
       android:id="@+id/monthly_attendance"
       android:textColor="#FFFFFF"
       android:layout_height="150dp"
       android:layout_width="150dp"
       android:background="@drawable/green_round_button"
       android:text="@string/monthly_attendance"
       android:textSize="25sp"
       android:onClick="onMonthlyAttendance"
       android:layout_alignParentTop="true"
       android:layout_alignParentRight="true"
       android:layout_alignParentEnd="true" />
    <Button
        android:id="@+id/defaulters"
        android:layout_height="150dp"
        android:layout_width="150dp"
        android:background="@drawable/red_round_button"
        android:text="@string/defaulters"
        android:textSize="25sp"
        android:onClick="onDefaulters"
        android:layout_below="@+id/total_attendance"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="85dp" />
</RelativeLayout>
